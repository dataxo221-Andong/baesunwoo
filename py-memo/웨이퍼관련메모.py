### 임시 (웨이퍼 데이터 구조 추가 고안)

### **공정 추적성 (Traceability): 장비 및 레시피 정보**

불량 유형(`failureType`)을 아는 것의 최종 목적은 **"어떤 장비가 범인인지 잡는 것"**입니다.

- **현재:** "이 웨이퍼는 `Donut` 불량입니다."
- **엔지니어의 의문:** "그래서 이거 어느 챔버(Chamber)에서 구운 거야? A장비야 B장비야?"
- **추가 제안:**
    - **`EquipmentID` (장비명):** 예: `EQP-Deposition-01`
    - **`ProcessTimestamp` (처리 시간):** 언제 작업했는지 알아야 전후 웨이퍼와 비교 가능.
    - **`StepID` (공정 단계):** 식각(Etch) 단계인지, 증착(Deposition) 단계인지.

### **사용자 피드백 (Human Validation) - 시간 남을 때**

AI가 `Confidence`가 낮거나, 틀렸을 때 사람이 정정할 수 있어야 데이터가 쌓이고 모델이 똑똑해집니다.

- **현재:** AI가 뱉은 `failureType`만 있음.
- **엔지니어의 행동:** "이거 `Loc` 아니고 `Random`인데 틀렸네." (하고 넘어감 -> 데이터 증발)
- **추가 제안:** **`IsVerified` (검증 여부)** 및 **`UserLabel` (사용자 정정 라벨)**
    - 초기값은 `False` / `Null`.
    - 엔지니어가 "확인" 버튼을 누르면 `True`.
    - AI가 틀려서 엔지니어가 라벨을 바꾸면 그 값을 저장. (나중에 재학습 데이터로 활용)