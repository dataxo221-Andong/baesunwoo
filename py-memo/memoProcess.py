1. [입력] 웨이퍼 이미지 수신

- 상황: 프론트엔드에서 사용자가 촬영하거나 업로드한 원본 웨이퍼 이미지가 백엔드 서버로 들어옴
- 상태: 해상도 제각각, 파일 포맷 제각각, 용량 큼

2. [전처리] "32x32" 표준화 및 색상 변환 (핵심)

- 서버가 이미지를 받자마자 32x32 픽셀 크기로 강제 축소(리사이징)합니다.
- 동시에 색상 규칙을 재설정합니다.
    - 원본의 불량 지역은 노란색으로 칠함.
    - 원본의 양품 구역은 빨간색으로 칠함.
    - 나머지 배경은 검은색 유지.
- 결과: 이제부터 이 **"32x32 크기의 검/빨/노 이미지"**가 우리 시스템의 진짜 원본 데이터가 됩니다. (진짜 원본 고화질 사진은 여기서 폐기)

3. [분석] AI 모델 판독

- 위에서 만든 32x32 이미지를 AI 모델(`wafer_classifier`)에 집어넣습니다.
- 결과 도출:
    - 불량 유형: 예) `Donut`
    - 신뢰도: 예) `98.5%`
    - 칩 개수: 예) `1024`
    - 불량 개수: 예) `1`
    - 불량 비율: 예) `0.985`
    - 전체 등급: 예) `A`

4. [저장] DB 적재 (최종)

- DB의 `wafer_logs` 테이블에 한 줄을 생성하고 데이터를 넣습니다.
    1. `lotName`: 로트 번호
    2. `waferMap`: 2번 단계에서 만든 32x32 이미지를 문자열(Base64)로 변환한 것.
    3. `waferSize`: 웨이퍼 크기 (예: 32x32)
    4. `failureType`: AI가 알아낸 불량 이름 (`Donut`)
    5. `confidence`: AI의 확신 확률 (`0.985`)
    6. `dieCount`: 칩 개수 (`1024`)
    7. `defectCount`: 불량 칩 개수 (`1`)
    8. `defectDensity`: 불량 밀도 (`0.985`)
    9. `totalGrade`: 전체 등급 (`A`)
    10. `created_at`: 생성일 (DB Insert Time)

[요약] 들어온 사진을 바로 32x32로 줄이고 색깔 바꿔서 AI 돌린 다음, 그 작은 이미지를 문자열로 바꿔서 결과랑 같이 DB에 넣고 끝. (원본 사진은 저장 안 함)